# DNA meaning layer: L1 and L2 lane report

Date: 2026-09-02  
Branch: `meaning/L1-L2`

## Result

All planned code and test deliverables are complete.

The three branches track `origin/meaning/L1-L2`.

This lane does not deploy the product or rebuild mirrors.

## bio-core

Baseline `main`: 93 passed, 1 skipped.  
Lane result: 98 passed, 1 skipped.

The skipped test requires the optional `allel` package.

Commits:

- `674a9c2` Report zygosity and call quality as fields of a carried variant
- `40c0334` Give a Finding a typed place for its meaning and evidence chain
- `e37490d` Attribute findings from the full ClinVar mirror

The carried-variant record now includes zygosity, filter, QUAL, GQ, and depth.

`Finding` now carries typed interpretation, evidence-chain, promotion, and deeper-dive fields.

## GeneAsk

Baseline `main`: 82 passed.  
Lane result: 90 passed.

Commits:

- `c0b1ff6` ClinVar mirror v2: keep the condition, its ids, the consequence and the origin
- `5dbcca6` Document the ClinVar mirror v2 rebuild
- `8b2da6f` Carry the condition, consequence and call quality into a ClinVar finding
- `453ddb2` GWAS mirror v2: keep the effect size, its interval, the study and whether the allele is carried
- `fd5186f` gnomAD mirror v2: keep allele counts and per-population frequencies; say chromosomes, not people

ClinVar, GWAS, and gnomAD mirrors now use schema version 2.

Their readers refuse older schemas instead of serving partial records.

The ClinVar screen now preserves condition, consequence, origin, allele, genotype, zygosity, and call-quality fields.

GWAS findings now preserve effect values, intervals, accessions, samples, mapped genes, p-value text, and carried-allele state.

gnomAD findings now preserve counts, homozygote counts, population frequencies, and release version.

## dna-report

Baseline `main`: 240 passed, 1 skipped, 1 warning.  
Lane result: 252 passed, 1 skipped, 1 warning.

The skipped test requires an object-store configuration.

The warning comes from Starlette's pending multipart import deprecation.

Commits:

- `e6ad822` JSON 2.0: the whole finding, its meaning and its chain, with nothing dropped
- `4ccfc6e` Markdown leads with what to read first and keeps every link and meaning
- `712c1bc` MCP can answer what matters, one finding, and filter by condition
- `984465d` Say what did not run, say what was capped, and withhold covariates from every format
- `9cef385` One BRCA2 finding must read the same in JSON, Markdown and MCP

JSON 2.0 keeps the full finding and retains the 1.x convenience keys.

Markdown 2.0 derives finding content from JSON 2.0.

MCP supports promoted findings, marker lookup, and meaning-aware filters.

Empty reports name unavailable providers and show their notes.

The methylation cap appears as a note and a structured limit.

JSON and Markdown publication now share one covariate-filtered composition path.

The BRCA2 fixture passes through extraction, screening, JSON, Markdown, and MCP.

## Vendor header checks

No current vendor header is reachable on this host.

The local `/data/gwas/gwas_assoc.tsv` path is absent.

Network access cannot fetch a header-only copy.

The GWAS code retains these planned, unverified indices:

- `INITIAL SAMPLE SIZE`: 8
- `REPLICATION SAMPLE SIZE`: 9
- `MAPPED_GENE`: 14
- `P-VALUE (TEXT)`: 29
- `OR or BETA`: 30
- `95% CI (TEXT)`: 31
- `STUDY ACCESSION`: 36

The code marks these positions as unverified.

The local gnomAD sites VCF headers are absent.

The gnomAD ingester captures every encountered `AF_*` key dynamically.

The code marks the current population-label set as unverified.

The local ClinVar source VCF header is absent.

The ClinVar code retains the planned `ORIGIN` bitmask labels.

## Plan adjustments

The ClinVar test excludes identifiers attached to placeholder condition groups.

The implementation pairs `CLNDN` and `CLNDISDB` groups before filtering placeholders.

The MCP tests use a valid 32-character report identifier and the repository's ready-state wrapper.

The methylation cap test uses a beta-matrix fixture and patches `Registry.annotate`, which matches the current engine boundary.

The empty-state and cap strings use active present voice.

They therefore differ from the passive past-tense examples in the plan.

The CLI produces HTML only, so it has no JSON or Markdown export caller to redirect.

The web and worker JSON and Markdown callers use `compose_result_views`.

## Open operations

Vendor-header verification remains open.

The required files are absent locally, and network access is unavailable.

Mirror rebuilds remain open as scheduled operations.

The approved plan excludes mirror rebuilds from this lane.

No other lane deliverable remains open.
